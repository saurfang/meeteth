import React from "react";

export default class Manage extends React.Component {
  public render() {
    return (
      <>
        Create new calendar
        Manage existing calendar
      </>
    );
  }
}